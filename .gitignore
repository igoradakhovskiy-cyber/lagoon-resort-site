# OS
.DS_Store
Thumbs.db

# Editor / local tooling
.claude/
.vscode/
*.log

# Tilda paste-ready fragments (local build artifacts, not part of the live site)
tilda/

# Internal strategy documents (DNA, Hunt ladder, offers) — never publish
strategy/
